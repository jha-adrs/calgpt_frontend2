// Main navbar component
// Has two navbars, one at left side, one at top

import React from 'react'

const Navbar = () => {
  return (
    <div>
      Top Nav
      {/**Top Nav */}
      {/**Left Nav */}
    </div>
  )
}

export default Navbar
